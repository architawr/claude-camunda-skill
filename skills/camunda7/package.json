{
  "name": "camunda-skill-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Bundled helpers for the camunda7 skill: read, lay out (preserving camunda: extensions), validate, lint, diff, and find executable Camunda 7 BPMN files.",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:coverage": "node --test --experimental-test-coverage test/*.test.mjs"
  },
  "dependencies": {
    "bpmn-auto-layout": "^0.5.0",
    "bpmn-moddle": "^9.0.1",
    "camunda-bpmn-moddle": "^7.0.1"
  }
}
